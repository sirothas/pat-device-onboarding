# Security

Report a vulnerability privately to **security@parvatiat.com**. Do not open a public issue.

This repository is public on purpose: it holds only the installer. It must never contain a secret,
a hostname, or a client's name - client values arrive at run time in the PAT-signed tenant
document. Anything committed here is permanent; secret scanning runs on every push.
